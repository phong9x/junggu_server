/*
 * Created on 22 Feb 2016 ( Time 14:37:23 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.trams.junggu.web.controller;

import java.util.List;
import java.util.ArrayList;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.data.domain.Page;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.trams.junggu.web.common.AbstractController;
import org.trams.junggu.web.common.FormMode;
import org.trams.junggu.web.common.Pager;
import org.trams.junggu.web.common.Login;
import org.trams.junggu.web.common.Message;
import org.trams.junggu.web.common.MessageType;
import org.trams.junggu.bean.jpa.ServerEntity;

//--- Entities
import org.trams.junggu.bean.Server;

//--- Services 
import org.trams.junggu.business.service.ServerService;


/**
 * Spring MVC controller for 'Server' management.
 */
@Controller
@RequestMapping("/server")
public class ServerController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "server";
	private static final String MAIN_LIST_NAME = "list";

	private static final String TOTAL_PAGE   = "pages";

	private static final String CURRENT_PAGE   = "pageNumber";

	private static final String LIST_PAGES   = "listPages";

	private static final Integer PAGE_SIZE   = 15;

	private static String nav = "server";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "server/form";
	private static final String JSP_LIST   = "server/list";
	private static final String JSP_PAGING   = "server";
	private static final String JSP_DETAIL   = "server/detail";
	private static final String JSP_LOGIN   = "login/index";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/server/create";
	private static final String SAVE_ACTION_UPDATE   = "/server/update";

	//--- Main entity service
	@Resource
    private ServerService serverService; // Injected by Spring
	//--- Other service(s)

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ServerController() {
		super(ServerController.class, MAIN_ENTITY_NAME );
		log("ServerController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------

	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param server
	 */
	private void populateModel(Model model, Server server, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, server);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Server found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/list")
	public String list(Model model) {
		/*	if(Login.checkLogin()=="0")
			return JSP_LOGIN;
		*/
		//List<Server> list = serverService.findAll();
		model.addAttribute(MAIN_LIST_NAME, null);		
		return JSP_LIST;
	}

	@RequestMapping()
	public String serverList(@RequestParam(value="page", defaultValue="1") Integer pageNumber, Model model) {
		
	/*	if(Login.checkLogin()=="0")
			return JSP_LOGIN;
	*/
		Page<ServerEntity> listPage = serverService.findAll(pageNumber);
		
		model.addAttribute("activePage", nav);
		
		new Pager<ServerEntity>(listPage).setSetting(model, "");	
		
		return JSP_PAGING;
	}


	@RequestMapping("/detail/{id}")
	public String detail(Model model, @PathVariable("id") Integer id) {
		
		Server server = serverService.findById(id);
		model.addAttribute("server", server);
		return JSP_DETAIL;
	}
	/**
	 * Shows a form page in order to create a new Server
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
	if(Login.checkLogin()=="0")
			return JSP_LOGIN;
	
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Server server = new Server();
		populateModel( model, server, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Server
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
	if(Login.checkLogin()=="0")
			return JSP_LOGIN;
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Server server = serverService.findById(id);
		populateModel( model, server, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param server  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Server server, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		if(Login.checkLogin()=="0")
			return JSP_LOGIN;
		try {
			if (!bindingResult.hasErrors()) {
				Server serverCreated = serverService.create(server); 
				model.addAttribute(MAIN_ENTITY_NAME, serverCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, server.getId() );
			} else {
				populateModel( model, server, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "server.error.create", e);
			populateModel( model, server, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param server  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Server server, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		if(Login.checkLogin()=="0")
			return JSP_LOGIN;
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Server serverSaved = serverService.update(server);
				model.addAttribute(MAIN_ENTITY_NAME, serverSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, server.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, server, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "server.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, server, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {

		if(Login.checkLogin()=="0")
			return JSP_LOGIN;
		log("Action 'delete'" );
		try {
			serverService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "server.error.delete", e);
		}
		return redirectToList();
	}

}
